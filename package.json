{
  "name": "promptprofit-platform",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "dev": "pnpm --filter website dev",
    "build": "pnpm --filter website build",
    "start": "pnpm --filter website start",
    "build:all": "pnpm -r build"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.110.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "supabase": "^2.109.0"
  }
}
